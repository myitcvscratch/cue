# Generated by internal/ci/ci_tool.cue; do not edit

name: Prevent PR merge
on:
- pull_request
jobs:
  fail:
    runs-on: ubuntu-18.04
    defaults:
      run:
        shell: bash
    steps:
    - run: echo Failing to prevent PR merge
    - run: "false"
